<Window x:Class="SecondBrain.App.TranscriptWindow" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Second Brain · Transcript" Style="{StaticResource ShellWindowStyle}" Width="920" Height="720" MinWidth="620" MinHeight="480" ShowInTaskbar="False" WindowStartupLocation="CenterOwner">
  <Grid Margin="20">
    <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/><RowDefinition Height="150"/></Grid.RowDefinitions>
    <TextBlock Text="Meeting transcript" Style="{StaticResource PageTitle}" />
    <TextBlock x:Name="Summary" Grid.Row="1" TextWrapping="Wrap" Foreground="{StaticResource Muted}" Margin="0,8,0,12" />
    <ListBox x:Name="Segments" Grid.Row="2" SelectionChanged="Segments_SelectionChanged" ScrollViewer.HorizontalScrollBarVisibility="Disabled" VirtualizingPanel.IsVirtualizing="True" VirtualizingPanel.VirtualizationMode="Recycling" HorizontalContentAlignment="Stretch" AutomationProperties.Name="Transcript segments">
      <ListBox.ItemTemplate><DataTemplate><StackPanel Margin="4,7">
        <TextBlock Text="{Binding Meta}" FontWeight="Bold" Foreground="{StaticResource Muted}" />
        <TextBlock Text="{Binding Text}" TextWrapping="Wrap" Margin="0,5,0,0" />
      </StackPanel></DataTemplate></ListBox.ItemTemplate>
    </ListBox>
    <TextBlock Grid.Row="3" Text="Selected segment · word timing" FontWeight="Bold" Margin="0,14,0,8" />
    <TextBox x:Name="WordDetails" Grid.Row="4" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" AutomationProperties.Name="Word timestamps" />
  </Grid>
</Window>
